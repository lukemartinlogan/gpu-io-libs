[project]
name = "gpu-hdf5-bench-data"
version = "0.1.0"
description = "Test data generation for GPU HDF5 benchmarks"
requires-python = ">=3.10"
dependencies = [
    "h5py>=3.0.0",
    "numpy>=1.20.0",
]

[tool.uv]
dev-dependencies = []
