[project]
name = "hdf5-test-generator"
version = "0.1.0"
description = "HDF5 test file generator and verification for GPU I/O testing"
requires-python = ">=3.10"
dependencies = [
    "h5py>=3.10.0",
    "numpy>=1.26.0",
]
