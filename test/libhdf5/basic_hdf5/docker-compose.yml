version: "3.8"
services:
  dev:
    build:
      context: ../../../../example
      dockerfile: Dockerfile.dev
    image: iowarp-dev
    volumes:
      - .:/project
    working_dir: /project
    tty: true
    stdin_open: true
